
/**
 * @brief This function is meant to be used in a thread so it can communicate back and forth with the python controller
 * 
 * @param controller controller address, used to acces dataModel
 * 
 */
void communicationLoop(void *pvParameters);

